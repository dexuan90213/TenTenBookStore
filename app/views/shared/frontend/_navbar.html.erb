<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to root_path, class: "navbar-item" do %>
      <i class="fas fa-book fa-2x"></i>
    <% end %>

    <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to '書本連結', books_path, class: 'navbar-item' %>
    </div>

    <div class="navbar-end">
      <div class="navbar-item" data-controller="cart" data-action="ajax:success@window->cart#update">
        <div class="buttons">
          <%= link_to cart_path, class: 'button' do %>
            <i class="fas fa-shopping-cart"></i>(<span data-target="cart.items"><%= current_cart.items.count %></span>)
          <% end %>
          <% if user_signed_in? %>
            <div class="navbar-item has-dropdown is-hoverable">
              <a class="navbar-link">
                <%= current_user.email %>
              </a>

              <div class="navbar-dropdown">
                <a class="navbar-item">
                  <%= link_to '訂單', orders_path, class: 'button' %>
                </a>
                <a class="navbar-item">
                  Jobs
                </a>
                <a class="navbar-item">
                  Contact
                </a>
                <hr class="navbar-divider">
                <a class="navbar-item">
                  <%= link_to '登出', destroy_user_session_path, class: 'button is-light', method: :delete %>
                </a>
              </div>
            </div>
        </div>
      <% else %>
        <%= link_to '註冊', new_user_registration_path, class: 'button is-primary' %>
        <%= link_to '登入', new_user_session_path, class: 'button is-light' %>
      <% end %>
      </div>
    </div>
  </div>
  </div>
</nav>
